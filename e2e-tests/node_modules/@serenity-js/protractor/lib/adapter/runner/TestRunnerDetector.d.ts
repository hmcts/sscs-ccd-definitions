import { TestRunnerAdapter } from '@serenity-js/core/lib/adapter';
import { Config as ProtractorConfig } from 'protractor';
import { TestRunnerLoader } from './TestRunnerLoader';
/**
 * Detects the {@apilink TestRunnerAdapter} to use,
 * based on Protractor configuration.
 *
 * @group Integration
 */
export declare class TestRunnerDetector {
    private readonly testRunnerLoader;
    static cucumberOpts: string;
    static jasmineNodeOpts: string;
    static mochaOpts: string;
    static protractorCliOptions(): string[];
    constructor(testRunnerLoader: TestRunnerLoader);
    runnerFor(config: ProtractorConfig): TestRunnerAdapter;
    private useJasmine;
    private useMocha;
    private useCucumber;
    private mergedConfigFor;
    private multiCapabilitiesOrTestShardingEnabled;
}
//# sourceMappingURL=TestRunnerDetector.d.ts.map