import { ActivityDetails, CorrelationId, Outcome } from '../model';
import { Timestamp } from '../screenplay';
import { DomainEvent } from './DomainEvent';
/**
 * Emitted when an {@apilink Activity} is finished.
 *
 * @group Events
 */
export declare abstract class ActivityFinished extends DomainEvent {
    readonly sceneId: CorrelationId;
    readonly activityId: CorrelationId;
    readonly details: ActivityDetails;
    readonly outcome: Outcome;
    constructor(sceneId: CorrelationId, activityId: CorrelationId, details: ActivityDetails, outcome: Outcome, timestamp?: Timestamp);
}
//# sourceMappingURL=ActivityFinished.d.ts.map