import { ActivityDetails, CorrelationId } from '../model';
import { Timestamp } from '../screenplay';
import { DomainEvent } from './DomainEvent';
/**
 * Emitted when an {@apilink Activity} starts.
 *
 * @group Events
 */
export declare abstract class ActivityStarts extends DomainEvent {
    readonly sceneId: CorrelationId;
    readonly activityId: CorrelationId;
    readonly details: ActivityDetails;
    constructor(sceneId: CorrelationId, activityId: CorrelationId, details: ActivityDetails, timestamp?: Timestamp);
}
//# sourceMappingURL=ActivityStarts.d.ts.map